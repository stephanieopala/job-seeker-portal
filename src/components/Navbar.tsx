import {
  Flex
} from "@radix-ui/themes"
const Navbar = () => {
  return (
    <Flex>
      <p>Logo</p>
      <div>
        <button>Jobs</button>
        <button>Sign Up</button>
      </div>
    </Flex>
  )
}

export default Navbar