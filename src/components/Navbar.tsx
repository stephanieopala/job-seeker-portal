const Navbar = () => {
  return (
    <nav>
      <p>Digital Journal</p>
      <div>
        <button>Sign In</button>
        <button>Sign Up</button>
        <button>Log Out</button>
      </div>
    </nav>
  )
}

export default Navbar